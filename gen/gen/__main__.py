import sys
from .ticket_office import TicketOffice
from .triangular import triangular

if __name__ == "__main__":
    # sys.stdin = open("/home/talos/devs/stuff/uw/se/se-task1/tests/example.in", "r")
    # office = TicketOffice()
    # for line in map(lambda _line: _line.rstrip("\n"), sys.stdin):
    #     office.run(line)
    # sys.stdout.write(f"{office.counter}\n")
    triangular()